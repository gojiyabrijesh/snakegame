# snakegame
 This is a classic Snake Game implemented in C language, console graphics (conio.h, dos.h, cprintf, gotoxy). The game runs inside a console window and provides smooth movement, scoring, dynamic speed control, and collision detection.
